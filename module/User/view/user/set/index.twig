{% extends 'layout/layout.twig' %}
{% block pageTitle %}
    {{ PageTitle ('修改個人資料', {'icon':'ion-android-contact'}) }}
{% endblock %}
    {% block content %}
        <div class="panel">
            <div class="panel-body">
                <div class="row">
                    <div class="col-sm-2">

                        {% include 'user/menu.twig' %}
                    </div>
                    <div class="col-sm-8">
                        <p>您好! {{ username }}</p>
                        <p><span class="label-info">目前登入帳號:</span> {{ username }}</p>
                        <p><span class="label-info">目前可使用平台:</span> {{ type }}</p>
                        {{ form(form) }}
                    </div>
                </div>
            </div>
        </div>
    {% endblock content %}
